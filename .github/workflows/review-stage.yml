name: review-stage.yml
on:
  pull_request:
    branches:
      - main

jobs:
  publish:
    name: Review Source Code
    if: false
    runs-on: ubuntu-latest # 배포 환경에 맞게 수정해주어야 함
    steps:
      - name: Placeholder for future
        run: echo " Review Source Code 워크플로 추후 작성 예정"