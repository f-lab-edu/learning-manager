auth:
  jwt:
    secret: ${AUTH_JWT_SECRET}
    access-token:
      expiration-seconds: ${AUTH_JWT_ACCESS_TOKEN_EXPIRATION:1800}
      issuer: "lxm-prod-api"
      audience: "lxm-prod-auth"
    password-reset-token:
      issuer: "lxm-prod-api"
      audience: "lxm-prod-auth"
      validity-in-seconds: 300
  refresh-token:
    ttl-hours: ${AUTH_REFRESH_TOKEN_TTL_HOURS:168}  # 기본 7일 (168시간)